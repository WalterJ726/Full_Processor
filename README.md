## for the first time

```
ssh-keygen -t rsa -C "walterjiang726@gmail.com"

git config --global user.name "WalterJ726"
git config --global user.email "walterjiang726@gmail.com"

echo "# DukeProject" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/WalterJ726/DukeProject.git
git push -u origin main
```

## others

```
git remote add origin https://github.com/WalterJ726/DukeProject.git
git branch -M main
git push -u origin main
```

